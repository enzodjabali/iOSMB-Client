services:
  iosmb-client:
    build:
      context: .
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
    labels:
      - "com.iosmb.description=iOSMB Client - iOS Message Bridge"
